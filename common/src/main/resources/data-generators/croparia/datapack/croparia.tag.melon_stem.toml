registry = "croparia:melons"
path = "croparia/tags/block/stem.json"
type = "croparia:aggregated"
startup = true
content = '    "${stem}"'
template = """
{
  "replace": false,
  "values": [
${content}
  ]
}
"""
