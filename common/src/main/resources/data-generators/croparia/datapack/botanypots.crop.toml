registry = "croparia:crops"
path = "botanypots/recipe/${id.namespace}/seed/${id.path}.json"
dependencies = "botanypots"
template = """
{
  "type": "botanypots:crop",
  "seed": {
    "item": "${seed}"
  },
  "categories": [
    "crop", "farmland"
  ],
  "growthTicks": 1200,
  "display": {
    "type": "botanypots:aging",
    "block": "${crop_block}"
  },
  "drops": [
    {
      "chance": 1.00,
      "output": {
        "item": "${fruit}"
      }
    },
    {
      "chance": 0.01,
      "output": {
        "item": "${seed}"
      }
    }
  ]
}
"""