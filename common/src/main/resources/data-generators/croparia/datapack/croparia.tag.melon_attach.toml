registry = "croparia:melons"
path = "croparia/tags/block/attach.json"
type = "croparia:aggregated"
startup = true
content = '    "${attach}"'
template = """
{
  "replace": false,
  "values": [
${content}
  ]
}
"""
