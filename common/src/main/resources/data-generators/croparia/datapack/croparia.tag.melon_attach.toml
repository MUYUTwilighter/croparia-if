registry = "croparia:melons"
path = "croparia/tags/blocks/attach.json"
type = "croparia:aggregated"
startup = true
content = '    "${attach}"'
template = """
{
  "replace": false,
  "values": [
${content}
  ]
}
"""
