registry = "croparia:elements"
path = "${id.namespace}/items/${potion.path}.json"
startup = true
template = """
{
  "model": {
    "type": "minecraft:model",
    "model": "${id.namespace}:item/${potion.path}"
  }
}
"""