这是用于在不同 AI 对话中同步信息、规范 AI 行为的文件。当你阅读到此份文件时，你应当知晓：

- 你应当频繁阅读本文件，尽管对话中用户没有要求。
- /docs 下有对项目描述与 ai 对话的持久性信息概览，你应当阅读它们。
- 几个 gradle 模块 common/(neo)forge/fabric 下，java 与 resources
  目录都有着对项目设计与逻辑至关重要的文件。不要因一些文件看起来是数据 (如 *.json, *.toml) 而忽略它们。
- 如果开发主机环境为 Windows，由于可能的编码问题，统一使用 UTF-8。必要时，执行终端指令时先执行 `chcp 65001` 来保证编码统一。

当用户下达了需要对项目文件进行修改的指令，请遵循以下 SOP：

1. 执行操作；
2. 进行编译与测试（如果有无必要的情况，可自行裁定后通报用户），出现问题尝试修复；
3. 除非用户明确提出，否则自动提交本次执行所产生的更改；
4. 检查是否需要补充或创建文档，尤其需要在文档中标记哪些已实现；
5. 给出接下来可能的步骤。

如果步骤发生了意外情况（用户提供信息不足，或是出现无法处理的错误），暂停并向客户告知，同时尽可能地提供的操作选项。之后遵循用户指令。